name: Backend CI/CD

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'  

      - name: Install Dependencies
        run: |
          cd backend
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Test Import
        run: |
          cd backend
          python -c "print('Testing basic imports...'); import app; print('✅ App imports successfully')"

      - name: Run Health Check
        run: |
          cd backend
          python -c "from app import app; print('✅ App created successfully')"

      - name: Trigger Render Deployment
        if: github.ref == 'refs/heads/main'
        run: |
          echo "Deployment would be triggered here"
